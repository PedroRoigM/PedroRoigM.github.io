---
/**
 * Homepage — "Acerca de mí".
 *
 * Estructura (de arriba a abajo):
 *   1. Hero          — nombre, rol, lede (la frase del motor/datos), chips meta
 *   2. Acerca de     — bio corta + idiomas
 *   3. Ahora         — el TFG de F1 como card destacada → /f1
 *   4. Trayectoria   — timeline vertical con experiencia + educación
 *   5. Skills        — hard skills (chips) + soft skills (lista)
 *   6. CTA           — "Hablamos" → /contact
 *
 * Animaciones: se reutiliza el patrón `.reveal` + IntersectionObserver
 * que ya usan las secciones de F1 (fade + translateY 20→0, stagger vía
 * `--reveal-delay`). El hero tiene una entrada un pelín más larga
 * (split por líneas, duración hero).
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Footer from '../components/global/Footer.astro';

const locale = 'es' as const;

const content = {
  kicker: '// PEDRO ROIG',
  name: ['Pedro', 'Roig'],
  role: 'Full Stack Developer',
  roleSub: 'enfocado a datos y motorsport · Ing. del Software',
  lede:
    'Me mueve la intersección entre datos y el mundo del motor. Cómo la telemetría y el análisis en tiempo real pueden ganar una décima en una carrera. Mi objetivo: trabajar con los millones de puntos que genera un coche y convertirlos en decisiones que marquen la diferencia en pista.',
  meta: [
    { label: 'Madrid', href: null },
    { label: 'pedro.roig.morera@gmail.com', href: 'mailto:pedro.roig.morera@gmail.com' },
    { label: 'linkedin.com/in/pedro-roig-morera', href: 'https://linkedin.com/in/pedro-roig-morera' },
  ],

  aboutKicker: '// 01 — ACERCA DE',
  aboutTitle: 'Quién soy',
  aboutBody:
    'Full Stack Developer (Java + TypeScript + React) en Ermine, con foco en datos y motorsport. Ing. del Software por la U-tad. Combino código de producto con experimentación en simulación y ML cuando puedo.',
  langsTitle: 'Idiomas',
  langs: [
    { name: 'Español', level: 'Nativo' },
    { name: 'Inglés', level: 'C1 Advanced' },
    { name: 'Catalán', level: 'Nativo' },
  ],

  nowKicker: '// 02 — AHORA',
  nowTitle: 'Lo siguiente',
  nowSubtitle: 'Master en Ingeniería de Vehículos de Competición',
  nowOrg: 'Universidad Nebrija',
  nowPeriod: 'Próximamente · 2026 — 2027',
  nowBody:
    'Programa especializado en ingeniería de motorsport: dinámica vehicular, sistemas de propulsión, aerodinámica y telemetría. La continuación natural del TFG de F1 — esta vez con las manos en los datos en pista.',
  nowTags: ['Motorsport', 'Dinámica vehicular', 'Propulsión', 'Aerodinámica', 'Telemetría'],

  timelineKicker: '// 03 — TRAYECTORIA',
  timelineTitle: 'Experiencia y formación',
  timeline: [
    {
      period: 'Sep 2026 — Actualidad',
      title: 'Full Stack Developer',
      org: 'Ermine',
      points: [
        'Promovido de prácticas a FTE tras 6 meses; lidero proyectos end-to-end con Java, TypeScript y React',
        'Introduje Claude Code y un primer flujo de agentes en el equipo, ahora usado por 4 personas',
        'Lidero la migración de un servicio legacy a una arquitectura con agentes automatizados',
        'Mentoreo al equipo de prácticas en revisiones de código y prácticas de ingeniería',
      ],
      tech: ['Java', 'TypeScript', 'React', 'Claude Code', 'Agentes'],
      featured: true,
    },
    {
      period: 'Mar 2026 — Ago 2026',
      title: 'Prácticas · Full Stack Developer',
      org: 'Ermine (promoción a FTE al finalizar)',
      points: [
        '6 meses de prácticas full-stack con Java (backend), TypeScript y React',
        'Apliqué Claude Code por primera vez en un proyecto real y dejé plantillas internas reutilizables',
        'Entregué 3 features completas con su propio CI, monitorización y métricas',
        'Pasé a FTE en septiembre por iniciativa del equipo, no por extensión automática',
      ],
      tech: ['Java', 'TypeScript', 'React', 'Claude Code'],
    },
    {
      period: '06/2025 — 09/2025',
      title: 'Prácticas · Desarrollo Web',
      org: 'Tracehold',
      points: [
        'Desarrollé una app web de gestión con Next.js, Node.js y MongoDB',
        'Modelé y ajusté esquemas NoSQL (4 colecciones, ~30k docs) para reducir latencia de queries en ~40%',
        'Trabajé en equipo con sprints quincenales y dailies',
      ],
      tech: ['Next.js', 'Node.js', 'MongoDB'],
    },
    {
      period: '2022 — 2026',
      title: 'Ing. del Software · Mención Datos',
      org: 'Universidad de Tecnología y Arte digital (U-tad)',
      points: [
        'TFG: sistema multi-agente que entrenó una IA a correr un F1 simulado desde cero',
        'TFG validado empíricamente contra datos reales de F1 (3 Grandes Premios de 2024)',
      ],
      tech: ['TFG'],
    },
  ],

  skillsKicker: '// 04 — HABILIDADES',
  skillsTitle: 'Habilidades',
  hardSkills: [
    'Python',
    'SQL & Database Management',
    'Data Processing',
    'Data Analysis',
    'Pandas · Machine Learning',
    'Data Visualization',
    'Time-Series Analysis',
    'Matplotlib',
    'AWS',
    'Software Development',
    'Git',
  ],
  softSkillsTitle: 'Soft skills',
  softSkills: [
    'Comunicación',
    'Trabajo en equipo',
    'Adaptación',
    'Resolución bajo presión',
    'Atención al detalle',
  ],

  ctaKicker: '// ¿HABLAMOS?',
  ctaTitle: '¿Te interesa colaborar?',
  ctaBody:
    'Disponible para colaboraciones puntuales en proyectos de datos o motorsport. Si tienes algo entre manos, escríbeme.',
  ctaBtn: 'Contacto',
  ctaHref: '/contact',

  // TFG highlight — primer bloque visible tras el hero. Pensado para que
  // un reclutador vea en 5 segundos: qué es, qué cifras tiene, y dónde
  // está el código. Las 3 cifras vienen de validationData / StrategyData.
  // NOTA: el link a GitHub es un placeholder — sustituye por el repo real.
  tfgKicker: '// MI PROYECTO PRINCIPAL · TFG',
  tfgTitle: 'Una IA que corre, compite y se valida con datos reales de F1',
  tfgLede:
    'Simulador de F1 con físicas realistas + IA con Reinforcement Learning que entrena desde cero. Validado empíricamente contra los Grandes Premios de Barcelona, Bahrain y Monza de 2024.',
  tfgStats: [
    { value: '124s → 66s', label: 'vuelta rápida media', sublabel: 'en 98 generaciones' },
    { value: '75.6%', label: 'finalizaciones TOP 3', sublabel: 'vs. pilotos reales (180 carreras)' },
    { value: '3', label: 'circuitos validados', sublabel: 'estategias coinciden con la parrilla' },
  ],
  tfgCtaRepo: 'https://github.com/PedroRoigM/pedro-roig-r25-f1',
  tfgCtaRepoLabel: 'Ver código en GitHub',
  tfgCtaPortfolio: '/f1',
  tfgCtaPortfolioLabel: 'Ver portfolio completo',
};
---

<BaseLayout
  title="Pedro Roig Morera — Full Stack Developer (datos y motorsport)"
  description="Full Stack Developer (Java + TypeScript + React) con foco en datos y motorsport. Ing. del Software por la U-tad."
  locale={locale}
>
  <main class="home">
    {/* ============================================================
       Hero
       ============================================================ */}
    <section class="home__hero">
      <div class="home__hero-inner">
        <span class="home__hero-kicker hero-reveal" style="--hero-delay: 0ms">
          {content.kicker}
        </span>
        <h1 class="home__hero-name">
          {content.name.map((line, idx) => (
            <span class="home__hero-name-line hero-reveal" style={`--hero-delay: ${120 + idx * 90}ms`}>
              {line}
            </span>
          ))}
        </h1>
        <p class="home__hero-role hero-reveal" style="--hero-delay: 320ms">
          <span class="home__hero-role-main">{content.role}</span>
          <span class="home__hero-role-sub">{content.roleSub}</span>
        </p>
        <p class="home__hero-lede hero-reveal" style="--hero-delay: 440ms">
          {content.lede}
        </p>
        <ul class="home__hero-meta hero-reveal" style="--hero-delay: 560ms">
          {content.meta.map((m) => (
            <li>
              {m.href ? (
                <a href={m.href} class="home__chip" target={m.href.startsWith('http') ? '_blank' : undefined} rel={m.href.startsWith('http') ? 'noopener' : undefined}>
                  {m.label}
                </a>
              ) : (
                <span class="home__chip home__chip--static">{m.label}</span>
              )}
            </li>
          ))}
        </ul>
      </div>
    </section>

    {/* ============================================================
       TFG highlight — primer bloque visible tras el hero. Pensado para
       que un reclutador vea en 5 segundos: qué es, qué cifras tiene,
       y dónde está el código. El link a GitHub es placeholder —
       sustituye `tfgCtaRepo` por el repo real.
       ============================================================ */}
    <section class="home__tfg" aria-labelledby="home-tfg-title">
      <div class="home__tfg-inner">
        <span class="home__tfg-kicker reveal" style="--reveal-delay: 0ms">
          {content.tfgKicker}
        </span>
        <h2 id="home-tfg-title" class="home__tfg-title reveal" style="--reveal-delay: 80ms">
          {content.tfgTitle}
        </h2>
        <p class="home__tfg-lede reveal" style="--reveal-delay: 160ms">
          {content.tfgLede}
        </p>
        <ul class="home__tfg-stats reveal" style="--reveal-delay: 240ms">
          {content.tfgStats.map((s, i) => (
            <li class="home__tfg-stat" style={`--reveal-delay: ${320 + i * 80}ms`}>
              <div class="home__tfg-stat-value">{s.value}</div>
              <div class="home__tfg-stat-label">{s.label}</div>
              <div class="home__tfg-stat-sub">{s.sublabel}</div>
            </li>
          ))}
        </ul>
        <div class="home__tfg-actions reveal" style="--reveal-delay: 600ms">
          <a
            href={content.tfgCtaRepo}
            class="home__tfg-btn home__tfg-btn--primary"
            target="_blank"
            rel="noopener"
          >
            <span aria-hidden="true">⟨/⟩</span>
            {content.tfgCtaRepoLabel}
          </a>
          <a href={content.tfgCtaPortfolio} class="home__tfg-btn home__tfg-btn--secondary">
            {content.tfgCtaPortfolioLabel}
            <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </section>

    {/* ============================================================
       About
       ============================================================ */}
    <section class="home__section" aria-labelledby="home-about-title">
      <div class="home__container">
        <div class="home__about">
          <div class="home__about-text">
            <span class="home__kicker reveal" style="--reveal-delay: 0ms">
              {content.aboutKicker}
            </span>
            <h2 id="home-about-title" class="home__title reveal" style="--reveal-delay: 60ms">
              {content.aboutTitle}
            </h2>
            <p class="home__body reveal" style="--reveal-delay: 120ms">
              {content.aboutBody}
            </p>
          </div>
          <aside class="home__langs reveal" style="--reveal-delay: 180ms">
            <h3 class="home__langs-title">{content.langsTitle}</h3>
            <ul class="home__langs-list">
              {content.langs.map((l, i) => (
                <li class="home__lang-item" style={`--reveal-delay: ${i * 60}ms`}>
                  <span class="home__lang-name">{l.name}</span>
                  <span class="home__lang-level">{l.level}</span>
                </li>
              ))}
            </ul>
          </aside>
        </div>
      </div>
    </section>

    {/* ============================================================
       Now / Featured project
       ============================================================ */}
    <section class="home__section" aria-labelledby="home-now-title">
      <div class="home__container">
        <div class="home__now-head">
          <span class="home__kicker reveal" style="--reveal-delay: 0ms">
            {content.nowKicker}
          </span>
          <h2 id="home-now-title" class="home__title reveal" style="--reveal-delay: 60ms">
            {content.nowTitle}
          </h2>
        </div>

        <div class="home__now-card reveal" style="--reveal-delay: 120ms">
          <div class="home__now-card-head">
            <div>
              <p class="home__now-period">{content.nowPeriod}</p>
              <h3 class="home__now-title">{content.nowSubtitle}</h3>
              <p class="home__now-org">{content.nowOrg}</p>
            </div>
          </div>
          <p class="home__now-body">{content.nowBody}</p>
          <ul class="home__now-tags">
            {content.nowTags.map((t) => <li>{t}</li>)}
          </ul>
        </div>
      </div>
    </section>

    {/* ============================================================
       Timeline
       ============================================================ */}
    <section class="home__section" aria-labelledby="home-timeline-title">
      <div class="home__container">
        <div class="home__timeline-head">
          <span class="home__kicker reveal" style="--reveal-delay: 0ms">
            {content.timelineKicker}
          </span>
          <h2 id="home-timeline-title" class="home__title reveal" style="--reveal-delay: 60ms">
            {content.timelineTitle}
          </h2>
        </div>

        <ol class="home__timeline">
          {content.timeline.map((item, idx) => (
            <li
              class={`home__timeline-item reveal ${item.featured ? 'is-featured' : ''}`}
              style={`--reveal-delay: ${120 + idx * 90}ms`}
            >
              <span class="home__timeline-period">{item.period}</span>
              <h3 class="home__timeline-title">{item.title}</h3>
              <p class="home__timeline-org">{item.org}</p>
              <ul class="home__timeline-points">
                {item.points.map((p) => <li>{p}</li>)}
              </ul>
              {item.tech.length > 0 && (
                <ul class="home__timeline-tech">
                  {item.tech.map((t) => <li>{t}</li>)}
                </ul>
              )}
            </li>
          ))}
        </ol>
      </div>
    </section>

    {/* ============================================================
       Skills
       ============================================================ */}
    <section class="home__section" aria-labelledby="home-skills-title">
      <div class="home__container">
        <div class="home__skills-head">
          <span class="home__kicker reveal" style="--reveal-delay: 0ms">
            {content.skillsKicker}
          </span>
          <h2 id="home-skills-title" class="home__title reveal" style="--reveal-delay: 60ms">
            {content.skillsTitle}
          </h2>
        </div>

        <div class="home__skills-grid">
          <ul class="home__chips reveal" style="--reveal-delay: 120ms">
            {content.hardSkills.map((s, i) => (
              <li class="home__chip home__chip--skill" style={`--reveal-delay: ${i * 40}ms`}>
                {s}
              </li>
            ))}
          </ul>
          <aside class="home__soft reveal" style="--reveal-delay: 180ms">
            <h3 class="home__soft-title">{content.softSkillsTitle}</h3>
            <ul class="home__soft-list">
              {content.softSkills.map((s) => <li>{s}</li>)}
            </ul>
          </aside>
        </div>
      </div>
    </section>

    {/* ============================================================
       CTA
       ============================================================ */}
    <section class="home__section home__cta-section">
      <div class="home__container">
        <div class="home__cta reveal" style="--reveal-delay: 0ms">
          <span class="home__kicker">{content.ctaKicker}</span>
          <h2 class="home__cta-title">{content.ctaTitle}</h2>
          <p class="home__cta-body">{content.ctaBody}</p>
          <a href={content.ctaHref} class="home__cta-btn">
            {content.ctaBtn}
            <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer locale={locale} />
</BaseLayout>

{/* ================================================================
     Hero entrance + section reveal-on-scroll
     ================================================================ */}
<script>
  /**
   * Hero entrance — staggered fade + translateY for the headline lines.
   * Independent from the section reveal pattern so it can fire on
   * initial load (no IO needed) with a longer duration.
   */
  const initHero = () => {
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (reducedMotion) {
      document.querySelectorAll('.hero-reveal').forEach((el) => {
        (el as HTMLElement).style.opacity = '1';
        (el as HTMLElement).style.transform = 'none';
      });
      return;
    }
    requestAnimationFrame(() => {
      document.querySelectorAll('.hero-reveal').forEach((el) => {
        const delay = parseFloat((el as HTMLElement).style.getPropertyValue('--hero-delay')) || 0;
        window.setTimeout(() => el.classList.add('is-hero-visible'), delay);
      });
    });
  };

  /**
   * Section reveal — same pattern as F1 sections. Each `.home__section`
   * triggers reveal of its `.reveal` children when it scrolls into view.
   */
  const initReveals = () => {
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const reveals = document.querySelectorAll<HTMLElement>('.home .reveal');
    if (reducedMotion) {
      reveals.forEach((el) => el.classList.add('is-visible'));
      return;
    }
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (!entry.isIntersecting) continue;
          const target = entry.target as HTMLElement;
          const delayVar = parseFloat(target.style.getPropertyValue('--reveal-delay')) || 0;
          window.setTimeout(() => target.classList.add('is-visible'), delayVar);
          observer.unobserve(target);
        }
      },
      { threshold: 0.15, rootMargin: '0px 0px -6% 0px' }
    );
    reveals.forEach((el) => observer.observe(el));
  };

  const init = () => {
    initHero();
    initReveals();
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init, { once: true });
  } else {
    init();
  }
</script>

<style>
  /* ============================================================
     Layout & sectioning
     ============================================================ */
  .home {
    display: flex;
    flex-direction: column;
  }

  .home__container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-5);
    width: 100%;
  }

  @media (min-width: 768px) {
    .home__container {
      padding: 0 var(--space-8);
    }
  }

  .home__section {
    padding: var(--section-padding-y) 0;
  }

  @media (min-width: 768px) {
    .home__section {
      padding: var(--section-padding-y-lg) 0;
    }
  }

  /* ============================================================
     Typography helpers (shared across sections)
     ============================================================ */
  .home__kicker {
    display: inline-block;
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-brand);
    margin-bottom: var(--space-3);
  }

  .home__title {
    font-family: var(--font-display);
    font-weight: 600;
    line-height: 1.05;
    letter-spacing: var(--tracking-tight);
    color: var(--color-fg);
    margin: 0 0 var(--space-5);
    font-size: clamp(2rem, 4vw, 3rem);
  }

  .home__body {
    font-family: var(--font-body);
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-muted);
    max-width: 56ch;
    margin: 0;
  }

  /* ============================================================
     Hero
     ============================================================ */
  .home__hero {
    position: relative;
    padding: calc(var(--space-16) + var(--space-4)) 0 var(--section-padding-y);
    background:
      radial-gradient(
        ellipse 70% 60% at 50% 0%,
        rgba(168, 216, 0, 0.12) 0%,
        transparent 60%
      ),
      linear-gradient(180deg, var(--ink) 0%, var(--ink-2) 100%);
    overflow: hidden;
  }

  .home__hero::after {
    content: '';
    position: absolute;
    inset: auto 0 -1px 0;
    height: 1px;
    background: var(--rule);
  }

  .home__hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-5);
  }

  @media (min-width: 768px) {
    .home__hero-inner {
      padding: 0 var(--space-8);
    }
  }

  .home__hero-kicker {
    display: inline-block;
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-brand);
    margin-bottom: var(--space-5);
  }

  .home__hero-name {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(3rem, 9vw, 6.5rem);
    line-height: 0.95;
    letter-spacing: var(--tracking-tight);
    color: var(--color-fg);
    margin: 0 0 var(--space-6);
  }

  .home__hero-name-line {
    display: block;
  }

  .home__hero-role {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
    margin: 0 0 var(--space-6);
  }

  .home__hero-role-main {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-weight: 600;
    color: var(--color-fg);
    letter-spacing: var(--tracking-snug);
  }

  .home__hero-role-sub {
    font-family: var(--font-body);
    font-size: var(--text-md);
    color: var(--color-fg-muted);
    letter-spacing: 0;
  }

  .home__hero-lede {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-muted);
    max-width: 60ch;
    margin: 0 0 var(--space-8);
  }

  @media (min-width: 768px) {
    .home__hero-lede {
      font-size: var(--text-lg);
    }
  }

  .home__hero-meta {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  /* ============================================================
     Chip (hero meta + skills)
     ============================================================ */
  .home__chip {
    display: inline-flex;
    align-items: center;
    padding: var(--space-2) var(--space-3);
    background: var(--ink);
    border: 1px solid var(--rule);
    border-radius: var(--radius-md);
    font-family: var(--font-display);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-wide);
    color: var(--color-fg-muted);
    text-decoration: none;
    transition:
      border-color var(--motion-duration-short) var(--motion-ease-out),
      color var(--motion-duration-short) var(--motion-ease-out),
      transform var(--motion-duration-short) var(--motion-ease-out);
  }

  .home__chip:hover {
    border-color: var(--color-brand);
    color: var(--color-fg);
    transform: translateY(-1px);
  }

  .home__chip--static {
    cursor: default;
  }

  /* ============================================================
     TFG highlight — primer bloque post-hero, prominent.
     Pensado para vista en 5 segundos: kicker + título + 3 cifras + 2 CTAs.
     ============================================================ */
  .home__tfg {
    position: relative;
    padding: var(--section-padding-y-lg) 0;
    background:
      radial-gradient(
        ellipse 70% 80% at 50% 0%,
        rgba(168, 216, 0, 0.07) 0%,
        transparent 65%
      ),
      linear-gradient(180deg, var(--ink) 0%, var(--ink-2) 100%);
    overflow: hidden;
  }

  .home__tfg::before {
    content: '';
    position: absolute;
    inset: auto 0 0 0;
    height: 1px;
    background: var(--rule);
  }

  .home__tfg-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-5);
    text-align: center;
  }

  @media (min-width: 768px) {
    .home__tfg-inner { padding: 0 var(--space-8); }
  }

  .home__tfg-kicker {
    display: inline-block;
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-brand);
    margin-bottom: var(--space-5);
  }

  .home__tfg-title {
    font-family: var(--font-display);
    font-weight: 600;
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight);
    color: var(--color-fg);
    margin: 0 auto var(--space-5);
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    max-width: 28ch;
  }

  .home__tfg-lede {
    font-family: var(--font-body);
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-muted);
    margin: 0 auto var(--space-10);
    max-width: 60ch;
  }

  .home__tfg-stats {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-10);
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
  }

  @media (min-width: 640px) {
    .home__tfg-stats { grid-template-columns: repeat(3, 1fr); }
  }

  .home__tfg-stat {
    background: var(--ink-2);
    border: 1px solid var(--rule);
    border-radius: var(--radius-lg);
    padding: var(--space-6) var(--space-4);
    transition:
      border-color var(--motion-duration-short) var(--motion-ease-out),
      transform var(--motion-duration-short) var(--motion-ease-out);
  }

  .home__tfg-stat:hover {
    border-color: var(--color-brand);
    transform: translateY(-2px);
  }

  .home__tfg-stat-value {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    line-height: var(--leading-tight);
    color: var(--color-brand);
    letter-spacing: var(--tracking-tight);
    margin-bottom: var(--space-3);
  }

  .home__tfg-stat-label {
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    color: var(--color-fg);
    margin-bottom: var(--space-1);
  }

  .home__tfg-stat-sub {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    color: var(--color-fg-muted);
  }

  .home__tfg-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-3);
  }

  .home__tfg-btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-md);
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    text-decoration: none;
    transition:
      transform var(--motion-duration-short) var(--motion-ease-out),
      box-shadow var(--motion-duration-short) var(--motion-ease-out),
      border-color var(--motion-duration-short) var(--motion-ease-out);
  }

  .home__tfg-btn--primary {
    background: var(--color-brand);
    color: var(--ink);
  }

  .home__tfg-btn--primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 0 4px rgba(168, 216, 0, 0.18);
  }

  .home__tfg-btn--secondary {
    background: transparent;
    color: var(--color-fg);
    border: 1px solid var(--rule-strong);
  }

  .home__tfg-btn--secondary:hover {
    border-color: var(--color-brand);
    color: var(--color-brand);
    transform: translateY(-2px);
  }

  /* ============================================================
     About
     ============================================================ */
  .home__about {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-10);
  }

  @media (min-width: 768px) {
    .home__about {
      grid-template-columns: 1.5fr 1fr;
      gap: var(--space-12);
      align-items: start;
    }
  }

  .home__langs {
    background: var(--ink);
    border: 1px solid var(--rule);
    border-radius: var(--radius-md);
    padding: var(--space-5);
  }

  .home__langs-title {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-brand);
    margin: 0 0 var(--space-4);
    font-weight: 600;
  }

  .home__langs-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .home__lang-item {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--rule);
    opacity: 0;
    transform: translateY(8px);
    transition:
      opacity var(--motion-duration-moderate) var(--motion-ease-out),
      transform var(--motion-duration-moderate) var(--motion-ease-out);
    transition-delay: var(--reveal-delay, 0ms);
  }

  /* Reveal cascade: when the parent <aside> gets .is-visible from the
     IntersectionObserver, the children fade in with their per-item
     --reveal-delay (which the IntersectionObserver never sees directly
     on the <li>s, only on the parent). */
  .home__langs.is-visible .home__lang-item {
    opacity: 1;
    transform: translateY(0);
  }

  .home__lang-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .home__lang-name {
    font-family: var(--font-display);
    font-size: var(--text-sm);
    color: var(--color-fg);
  }

  .home__lang-level {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    color: var(--color-fg-muted);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  /* ============================================================
     Now card
     ============================================================ */
  .home__now-head {
    margin-bottom: var(--space-6);
  }

  .home__now-card {
    display: block;
    background: var(--ink);
    border: 1px solid var(--rule);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    color: inherit;
  }

  @media (min-width: 768px) {
    .home__now-card {
      padding: var(--space-8);
    }
  }

  .home__now-card-head {
    margin-bottom: var(--space-4);
  }

  .home__now-period {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-fg-muted);
    margin: 0 0 var(--space-2);
  }

  .home__now-title {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: var(--tracking-snug);
    color: var(--color-fg);
    margin: 0 0 var(--space-2);
  }

  .home__now-org {
    font-family: var(--font-body);
    font-size: var(--text-md);
    color: var(--color-brand);
    margin: 0;
  }

  .home__now-body {
    font-family: var(--font-body);
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-muted);
    margin: 0 0 var(--space-5);
    max-width: 64ch;
  }

  .home__now-tags {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .home__now-tags li {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    padding: var(--space-1) var(--space-3);
    background: var(--ink-2);
    border: 1px solid var(--rule);
    border-radius: var(--radius-sm);
    color: var(--color-fg);
    letter-spacing: var(--tracking-wide);
  }

  /* ============================================================
     Timeline
     ============================================================ */
  .home__timeline-head {
    margin-bottom: var(--space-8);
  }

  .home__timeline {
    list-style: none;
    padding: 0;
    margin: 0;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
  }

  .home__timeline::before {
    content: '';
    position: absolute;
    top: 8px;
    bottom: 8px;
    left: 7px;
    width: 1px;
    background: var(--rule);
  }

  @media (min-width: 768px) {
    .home__timeline::before {
      left: 9px;
    }
  }

  .home__timeline-item {
    position: relative;
    padding-left: var(--space-8);
  }

  .home__timeline-item::before {
    content: '';
    position: absolute;
    top: 8px;
    left: 0;
    width: 15px;
    height: 15px;
    border-radius: 50%;
    background: var(--ink);
    border: 2px solid var(--rule-strong);
    transition:
      border-color var(--motion-duration-moderate) var(--motion-ease-out),
      background var(--motion-duration-moderate) var(--motion-ease-out);
  }

  .home__timeline-item.is-featured::before {
    border-color: var(--color-brand);
    background: rgba(168, 216, 0, 0.15);
  }

  .home__timeline-period {
    display: block;
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-fg-muted);
    margin-bottom: var(--space-2);
  }

  .home__timeline-title {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-fg);
    margin: 0 0 var(--space-1);
    letter-spacing: var(--tracking-snug);
  }

  .home__timeline-org {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-brand);
    margin: 0 0 var(--space-3);
  }

  .home__timeline-points {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-3);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .home__timeline-points li {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    line-height: var(--leading-normal);
    color: var(--color-fg-muted);
    padding-left: var(--space-4);
    position: relative;
  }

  .home__timeline-points li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: var(--color-brand);
  }

  .home__timeline-tech {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .home__timeline-tech li {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    padding: var(--space-1) var(--space-3);
    background: var(--ink-2);
    border: 1px solid var(--rule);
    border-radius: var(--radius-sm);
    color: var(--color-fg);
    letter-spacing: var(--tracking-wide);
  }

  /* ============================================================
     Skills
     ============================================================ */
  .home__skills-head {
    margin-bottom: var(--space-8);
  }

  .home__skills-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-8);
  }

  @media (min-width: 768px) {
    .home__skills-grid {
      grid-template-columns: 2fr 1fr;
      gap: var(--space-10);
      align-items: start;
    }
  }

  .home__chips {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .home__chip--skill {
    opacity: 0;
    transform: translateY(8px) scale(0.96);
    transition:
      opacity var(--motion-duration-moderate) var(--motion-ease-out),
      transform var(--motion-duration-moderate) var(--motion-ease-out),
      border-color var(--motion-duration-short) var(--motion-ease-out),
      color var(--motion-duration-short) var(--motion-ease-out);
    transition-delay: var(--reveal-delay, 0ms);
  }

  /* Reveal cascade: when the parent <ul> gets .is-visible from the IO,
     the children fade in with their per-item stagger. */
  .home__chips.is-visible .home__chip--skill {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .home__chip--skill:hover {
    border-color: var(--color-brand);
    color: var(--color-fg);
  }

  .home__soft {
    background: var(--ink);
    border: 1px solid var(--rule);
    border-radius: var(--radius-md);
    padding: var(--space-5);
  }

  .home__soft-title {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--color-brand);
    margin: 0 0 var(--space-4);
    font-weight: 600;
  }

  .home__soft-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .home__soft-list li {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-fg);
    padding-left: var(--space-3);
    position: relative;
  }

  .home__soft-list li::before {
    content: '·';
    position: absolute;
    left: 0;
    color: var(--color-brand);
    font-weight: 700;
  }

  /* ============================================================
     CTA
     ============================================================ */
  .home__cta-section {
    padding-bottom: var(--section-padding-y-lg);
  }

  .home__cta {
    background: var(--ink);
    border: 1px solid var(--rule);
    border-radius: var(--radius-lg);
    padding: var(--space-8);
    text-align: center;
  }

  @media (min-width: 768px) {
    .home__cta {
      padding: var(--space-12) var(--space-10);
    }
  }

  .home__cta-title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    line-height: 1.1;
    letter-spacing: var(--tracking-tight);
    color: var(--color-fg);
    margin: 0 0 var(--space-4);
  }

  .home__cta-body {
    font-family: var(--font-body);
    font-size: var(--text-md);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-muted);
    max-width: 52ch;
    margin: 0 auto var(--space-6);
  }

  .home__cta-btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    background: var(--color-brand);
    color: var(--ink);
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition:
      transform var(--motion-duration-short) var(--motion-ease-out),
      box-shadow var(--motion-duration-short) var(--motion-ease-out);
  }

  .home__cta-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 0 4px rgba(168, 216, 0, 0.18);
  }

  .home__cta-btn span {
    transition: transform var(--motion-duration-short) var(--motion-ease-out);
  }

  .home__cta-btn:hover span {
    transform: translateX(4px);
  }

  /* ============================================================
     Reveal animations
     ============================================================ */
  .reveal {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity var(--motion-duration-long) var(--motion-ease-out),
      transform var(--motion-duration-long) var(--motion-ease-out);
    will-change: opacity, transform;
  }

  .is-visible.reveal {
    opacity: 1;
    transform: translateY(0);
  }

  /* Hero entrance — initial state (no flash of unstyled content) */
  .hero-reveal {
    opacity: 0;
    transform: translateY(24px);
    transition:
      opacity var(--motion-duration-hero) var(--motion-ease-out),
      transform var(--motion-duration-hero) var(--motion-ease-out);
    will-change: opacity, transform;
  }

  .hero-reveal.is-hero-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal,
    .hero-reveal,
    .home__lang-item,
    .home__chip--skill {
      opacity: 1 !important;
      transform: none !important;
      transition: none !important;
    }
  }
</style>
