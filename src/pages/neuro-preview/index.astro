---
/**
 * Temporary preview page — renders ONLY the NeuroevolutionSection so
 * Agent D can verify the section's visual output via Playwright without
 * touching Agent F's integration. DELETE after verification.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import NeuroevolutionSection from '../../components/sections/NeuroevolutionSection.astro';

const locale = 'es' as const;
---

<BaseLayout
  title="Neuroevolution preview"
  description="Preview of the neuroevolution training section."
  locale={locale}
>
  <NeuroevolutionSection locale={locale} />
</BaseLayout>